<Window x:Class="Contacts.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Contacts.View"
        xmlns:Converters="clr-namespace:Contacts.View.Services"
        xmlns:contact="clr-namespace:Contacts.View.Controls"
        xmlns:ViewModel="clr-namespace:Contacts.ViewModel;assembly=Contacts.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" MinHeight="500" MinWidth="700">

    <Window.Resources>
        <Style TargetType="ListBox">
            <Setter Property="Margin" Value="8,3,3,3"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3,3,3,8"/>
            <Setter Property="Height" Value="25px"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="VerticalAlignment"  Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>

        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3,3,8,3"/>
            <Setter Property="VerticalAlignment"  Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
        <DataTemplate x:Key="СontactsTemplate">
            <StackPanel Margin="3" Orientation="Horizontal">
                <Image Source="/Resources/photo_placeholder.png" Width="50" Height="50" HorizontalAlignment="Left"/>
                <StackPanel Margin="13" VerticalAlignment="Bottom">
                    <TextBlock FontSize="12" FontWeight="SemiBold" Text="{Binding Name}" />
                    <TextBlock FontSize="10" Text="{Binding PhoneNumber}"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <Converters:VisibilityConverter x:Key="VisibilityConverter" />

    </Window.Resources>

    <Window.DataContext>
        <ViewModel:MainVM/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200px"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="250px"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="#f8f8f8">
            <Grid.RowDefinitions>
                <RowDefinition Height="36px"/>
                <RowDefinition/>
                <RowDefinition Height="36px"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45px"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="Find" Grid.Column="0" Text="Find:" Height="19px" Margin="8,3,3,3"/>
                <TextBox x:Name="FindTextBox" Grid.Column="1" VerticalAlignment="Center" Margin="6,0,3,0"/>
            </Grid>
            <ListBox x:Name="ContactsListBox"  Grid.Row="1" ItemsSource="{Binding Contacts}" ItemTemplate="{StaticResource СontactsTemplate}"
                 Validation.ErrorTemplate="{x:Null}" SelectedItem="{Binding SelectedContact}">
            </ListBox>
            <Grid x:Name="ButtonsGrid" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button x:Name="AddButton" Content="Add" Grid.Column="0" Command="{Binding AddContactCommand}" Margin="8,3,3,8"/>
                <Button x:Name="EditButton" Content="Edit" Grid.Column="1" Command="{Binding EditContactCommand}"/>
                <Button x:Name="RemoveButton" Content="Remove" Grid.Column="2" Command="{Binding RemoveContactCommand}"/>
            </Grid>
        </Grid>
        <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Center" ShowsPreview="False"/>

        <Grid Grid.Column="2" Margin="8,8,8,8">
            <StackPanel>
                <contact:ContactUserControl 
                    IsReadOnly="{Binding DataContext.IsReadOnly, 
                        RelativeSource={RelativeSource AncestorType=local:MainWindow}}" 
                    DataContext="{Binding SelectedContact, ValidatesOnDataErrors=True}"/>

                <Button x:Name="ApplyButton"
                        Content="Apply"
                        Width="75px"
                        Command="{Binding ApplyChangesCommand}"
                        Visibility="{Binding IsApplyButtonVisibility, 
                            Converter={StaticResource VisibilityConverter}}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="113,50,3,3"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>